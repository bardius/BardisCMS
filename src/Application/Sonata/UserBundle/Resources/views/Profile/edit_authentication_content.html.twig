        <div class="panel panel-warning">
            <div class="panel-heading">
                <h3 class="panel-title">{{ 'title_user_edit_authentication'|trans({}, 'SonataUserBundle') }}</h3>
            </div>
            <div class="panel-body">
                <h3>Profile &amp; account settings</h3>
                <p>Edit your profile and account settings</p>
                {{ form_start(form, {'action': path('sonata_user_profile_edit_authentication'), 'method': 'POST', 'attr': {'novalidate': 'novalidate'} }) }}
                    {{ form_widget(form) }}
                    <button type="submit" name="submit" class="button">{{ 'sonata_user_submit'|trans({}, 'SonataUserBundle') }}</button>
                {{ form_end(form) }}
            </div>
        </div>

        <div class="panel panel-danger">
            <div class="panel-heading">
                <h3 class="panel-title">{{ 'sonata_change_password_link'|trans({}, 'SonataUserBundle') }}</h3>
            </div>
            <div class="panel-body">
                {% render url("sonata_user_change_password") %}
            </div>
        </div>
