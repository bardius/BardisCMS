{% if totalPages is defined %}
    {% if totalPages > 1 %}
        <div class="large-12 small-12 columns paginationHolder">
            <div class="row">
                <div class="small-12 large-12 columns">
                    <ul class="pagination text-center" role="navigation" aria-label="Pagination">
                        <li class="pagination-first arrow">
                            <a class="firstPageLink" href="{{ app.request.getBaseURL() }}/{{ page.alias }}/0/{% if totalpageitems %}{{ totalpageitems }}{% else %}0{% endif %}{% if linkUrlParams %}/{{ linkUrlParams|url_encode() }}{% endif %}" title="{{ 'form.search.pagin_first'|trans({}, 'SonataUserBundle') }}" aria-label="{{ 'form.search.pagin_first'|trans({}, 'SonataUserBundle') }}">
                                {{ 'form.search.pagin_first'|trans({}, 'SonataUserBundle') }}
                            </a>
                        </li>
                        {% if (currentpage - 1) >= 0 %}
                            <li class="pagination-previous arrow">
                                <a class="prevPageLink" href="{{ app.request.getBaseURL() }}/{{ page.alias }}/{{ currentpage - 1 }}/{% if totalpageitems %}{{ totalpageitems }}{% else %}0{% endif %}{% if linkUrlParams %}/{{ linkUrlParams|url_encode() }}{% endif %}" title="{{ 'form.search.pagin_previous'|trans({}, 'SonataUserBundle') }}" aria-label="{{ 'form.search.pagin_previous'|trans({}, 'SonataUserBundle') }}">
                                    {{ 'form.search.pagin_previous'|trans({}, 'SonataUserBundle') }}
                                </a>
                            </li>
                        {% endif %}
                        {% for i in 0..(totalPages - 1) %}
                            {% if i != currentpage %}
                                <li>
                                    <a class="pageNumber" href="{{ app.request.getBaseURL() }}/{{ page.alias }}/{{ i }}/{% if totalpageitems %}{{ totalpageitems }}{% else %}0{% endif %}{% if linkUrlParams %}/{{ linkUrlParams|url_encode() }}{% endif %}" title="Page {{ i }}" aria-label="Page {{ i }}">{{ i }}</a>
                                </li>
                            {% else %}
                                <li class="current">
                                    <a class="pageNumber" href="#" title="Page {{ i }}" aria-label="Page {{ i }}"><span class="show-for-sr">You're on page</span> {{ i }}</a>
                                </li>
                            {% endif %}
                        {% endfor %}
                        {% if (currentpage + 1) < totalPages %}
                            <li class="pagination-next arrow">
                                <a class="nextPageLink" href="{{ app.request.getBaseURL() }}/{{ page.alias }}/{{ currentpage + 1 }}/{% if totalpageitems %}{{ totalpageitems }}{% else %}0{% endif %}{% if linkUrlParams %}/{{ linkUrlParams|url_encode() }}{% endif %}" title="{{ 'form.search.pagin_next'|trans({}, 'SonataUserBundle') }}" aria-label="{{ 'form.search.pagin_next'|trans({}, 'SonataUserBundle') }}">
                                    {{ 'form.search.pagin_next'|trans({}, 'SonataUserBundle') }}
                                </a>
                            </li>
                        {% endif %}
                        <li class="pagination-last arrow">
                            <a class="lastPageLink" href="{{ app.request.getBaseURL() }}/{{ page.alias }}/{{ totalPages - 1 }}/{% if totalpageitems %}{{ totalpageitems }}{% else %}0{% endif %}{% if linkUrlParams %}/{{ linkUrlParams|url_encode() }}{% endif %}" title="{{ 'form.search.pagin_last'|trans({}, 'SonataUserBundle') }}" aria-label="{{ 'form.search.pagin_last'|trans({}, 'SonataUserBundle') }}">
                                {{ 'form.search.pagin_last'|trans({}, 'SonataUserBundle') }}
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    {% endif %}
{% endif %}
