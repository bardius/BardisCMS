    {{ form_start(form, {'action': path('fos_user_registration_register'), 'method': 'POST', 'attr': {'novalidate': 'novalidate'} }) }}
        <small class="right">{{ 'form.mandatory_fields'|trans({}, 'SonataUserBundle') }}</small>
        {{ form_widget(form) }}
        <input type="submit" id="subscribeFormBtn" class="button" value="{{ 'registration.submit'|trans({}, 'SonataUserBundle') }}" />
    {{ form_end(form) }}
