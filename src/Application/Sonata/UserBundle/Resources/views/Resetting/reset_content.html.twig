    <div class="row">
        <div class="large-12 small-12 columns">
        {% form_theme form with ['Form/fields.html.twig'] %}
        {{ form_start(form, {'action': path('fos_user_resetting_reset', {'token': token}), 'method': 'POST', 'attr': {'id': 'fos_user_resetting_form', 'novalidate': 'novalidate'} }) }}
            <div class="row">
                {{ form_widget(form) }}

                <div class="large-12 small-12 columns">
                    <button type="submit" name="submit" id="userRessetingFormBtn" class="button">{{ 'resetting.reset.submit'|trans({}, 'SonataUserBundle') }}</button>
                    <a class="button" href="{{ url('fos_user_registration_register') }}" title="{{ 'layout.link.create_account'|trans({}, 'SonataUserBundle') }}">
                        {{ 'layout.link.create_account'|trans({}, 'SonataUserBundle') }}
                    </a>
                    {% if formMessage is defined %}
                        <small class="form-message error">
                            {{ formMessage }}
                        </small>
                    {% endif %}
                </div>
            </div>
        {{ form_end(form) }}
        </div>
    </div>
