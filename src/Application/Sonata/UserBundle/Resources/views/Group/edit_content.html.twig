{#

Sonata User Bundle Overrides

This file is part of the BardisCMS.
Manage the extended Sonata User entity with extra information for the users

(c) George Bardis <george@bardis.info>

#}

<form action="{{ path('sonata_user_group_edit', {'groupname': groupname} ) }}" {{ form_enctype(form) }} method="POST" class="sonata_user_group_edit">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'group.edit.submit'|trans([], 'SonataUserBundle') }}" />
    </div>
</form>
