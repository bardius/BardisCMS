<div class="sonata_user_group_list">
    <ul>
    {% for group in groups %}
        <li><a href="{{ path('sonata_user_group_show', {'groupname': group.getName()} ) }}">{{ group.getName() }}</a></li>
    {% endfor %}
    </ul>
</div>
